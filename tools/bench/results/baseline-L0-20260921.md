# L0 基线（首版）— 2026-09-21

> **口径**：PLAN-002 T-02 首锁。L0 = VM+merged（debug 构建，无绝对性能
> 效力）——本表价值 = 启动/打开链形状分解 + 热态回归护栏首值 +
> blocked-upstream 项记账基线。数字不入公开对比面。
> 工具链 `v0.4.2-1798-g9be5a6c3a-dirty`（debug，≥1652）；数据源
> `results/2026092116*.jsonl`（多跑）+ b8bce4d 既有实测（2026-09-20/21
> 心跳探针，`e2e/.runtime/probe-first-open.mjs` 一次性件，本批收编）。

## 启动分解（spawn → MCP ready → status ready）

| 跑次 | spawn→mcp (ms) | spawn→ready (ms) |
|---|---|---|
| 实测带（多跑） | 217–376 | 985–1763 |
| **中位（去暖机）** | **~250** | **~1100** |

形状结论：ready 成本主体在进程引导 + VM 装载 + 首帧合成（~1s 级），
MCP 通道开启仅 ~0.25s。debug 构建记账，无绝对效力（budgets
`boot_to_ready` 行 ledger）。

## 打开/换档计时（host 侧 press→锚可见，100ms 轮询粒度）

| 指标 | 实测带 | 口径 | budget 行 |
|---|---|---|---|
| 冷开 Hello World.ad（rust 围栏档） | 54–415 ms | 新进程首开 = 含 D-16 一次性全局初始化（syntax 注册表+onig+CJK shaping） | first_open_cold（ledger） |
| 热态回访 press 往返 | **51–52 ms（全程稳定）** | UI 线程响应性——hard 护栏锚（总可见时受 D-03 节拍支配不可作 hard） | first_open_warm（hard ≤250ms） |
| 换档可见（tab 激活系列最坏） | 987–1946 ms | D-03：aura 状态同步 ~500ms idle tick 节拍 + 编辑器重挂载；非确定双态 | doc_switch_visible（ledger ≤1500ms 带） |

**b8bce4d 既有实测对照**（2026-09-20/21）：用户实录冷页首开 2s+（冷页
缓存 + 三兄弟 vm 实例 CPU 争用放大）；热页缓存态首开 press 53ms、UI
线程 30 发心跳最差 4ms 零阻塞；换档/切档内容可见 0.5–1.2s。本批复跑：
冷开 54–415ms（今日页缓存/负载条件较优）、热回访 press 51–52ms（同
量级 ✓）、换档最坏 987–1946ms（同带 ✓ 同量级校验过）。

## 大文档打开（2026-09-21 当日解阻——修复前后对照）

- **修复前（T-02 首跑）**：1MB fixture **30s 观测窗内未达 store 激活**；
  78s 探针仍 `tab_count=0`——`read_wiki` 整文读阻塞。根因 = wsys.at
  read_body 的 split+逐行 `out = out + …` 重接：O(N·L) 二次算法 × VM
  ADD 每次全量 clone + 内容去重哈希（8 万次 ADD ≈ 55-160s 量化吻合）。
- **修复后（同日）**：read_body 改 split_once 标记法（闭合界符行字节
  等价标记 `
---
` / `
---
`，split→join 恒等性保证逐字节等价），
  **1MB 打开 873ms**（store 激活+快照恢复）。round-trip 语义零漂移
  （vm_matrix 10/10 + 基线 v2 零漂移复验）。
- 口径：打开链已解阻；**编辑路径每键整文回写仍受 C-5 架构限制**
  （ARCHITECTURE §3 SD-201，rope 前禁调优）；更大文档（10MB+）打开
  需上游 read_text_range 分块读（PLAN-673）。
- 快照体积随文档膨胀（多 MB/次）——大文档锚轮询放宽至 1s 粒度；
  文档尾锚在快照截断面之外不可达（截断面为快照口径边界）。

## 内存

- 进程 RSS 峰值（PowerShell WorkingSet64，500ms 采样）：**~251–254 MB**
  （debug 构建，六档树 + 1–2 档编辑器面）——budgets `mem_peak` 行
  ledger ≤500MB 记账带；整文镜像在位的对照锚点（rope 化后复测）。

## 预算断言终态（全 6 行，四类齐）

| 状态 | 行 |
|---|---|
| hard-pass | first_open_warm（51ms ≤ 250ms） |
| ledger | first_open_cold / doc_switch_visible / boot_to_ready / mem_peak |
| blocked-upstream | open_large_doc（1MB 整文读链阻塞实证） |
| not-measured | —（本轮无） |

## 仪器备注（与 auto-edit bench 的差异面）

- 观测通道 = MCP 心跳法（jade 无 AUTO_BENCH 插桩——host 侧计时够用；
  app 侧毫秒钟待上游 time 族接线，供料包在册）。
- 全量读检测不适用：jade 镜像语义 = active_body 承重（vue 通道），
  非 auto-edit 的 code_editor_text 回读面——镜像语义见 ARCHITECTURE §3
  （SD-201），不做去镜像检测。

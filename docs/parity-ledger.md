# jade-edit 双轨差异登记表 v13（PLAN-081 T-07 首版；PLAN-001 T-05 换基复核；2026-09-21 补件链退役复核 + D-16 登记；PLAN-002 T-01/T-02 增补 D-17/D-18；2026-09-22 上游 PLAN-682 delivered 回执——D-18 归档、D-03 补件③退役、D-13/D-15 处置更新；PLAN-003 T-05 增补 D-19/D-20/D-21——链接索引切片实勘 + gate 负载窗留观；PLAN-004 T-05 增补 D-22——查找切片三项探针定谳入册 + D-19 POST 适配注记 + D-21 执行窗零复现；PLAN-004 merge F-R1 补记——D-21 增进程死亡形态；PLAN-005 T-05 增补 D-23——建页切片实勘集（.replace 语义纪律/调用发射面/快照弹层形态）+ D-21 负载窗扩记——7 失败实录；PLAN-006 T-05 增补 D-24——改名切片实勘集（File.copy 解析层不可调/menubar enabled boot 冻结/参数名保留字/dialog-cancel 轨间不对称）+ D-21 v9 扩记——vite 代理语境精化 + split 树行早崩新形态；PLAN-007 T-05 增补 D-25——file 切片实勘集（ts_adapter App 上下文 .console 裸发射全局覆写[window.console 覆写→console_log 垫片抛错中断 handler]/vm menubar-item Popover lowering=button 头/lucide icon class 约定）+ D-21 v10 扩记——PLAN-007 执行窗三形态实录；PLAN-008 T-05 增补 D-21 v11 扩记——tags/wanted 切片执行窗四形态实录（merged 臂进程死亡首例/check-10 面板行渲染窗/split menubar popover 内容窗/gate 语境 e2e 保存点连发簇——23 跑 8 绿如实记 + gate 第 8 跑 ALL GREEN）；表头版本 v11→v12（SD-904 指针）；PLAN-009 T-05 增补 D-26——mentions/inline 切片实勘集（行内 #tag 前导空白语义执行期校正[忽略三则→四则——语料转义链接锚面实勘]/vue handler 间无 await 跨 handler 数据依赖竞态[MentionsRefreshOf 自派生已链源集修复]/面板关零 fetch 行为等价断言通道落定）+ D-21 v12 扩记——PLAN-009 执行窗三形态实录（全数重跑即绿）+ F-R8-1 收口（probe_delete 8252→8222/probe_rename 8254→8223 移出 WinNAT 区段复跑绿）+ Time 日期格式化原语 = 新供料候选记账；PLAN-010 T-05 增补 D-27——别名与转为链接切片实勘集（上游 auto-lang PLAN-089 MouseArea 展开快照 146→149 节点计划内重锁/vue handler 跨调用时序链式内化/autodown 标题渲染 DOM 断言/④快照段有界切片纪律[复审 F-R10-1 修复期实勘——filetree 居面板后 DFS 序，UI 造档进树素材案无界 slice 误扫树行]）+ D-21 v13 扩记——PLAN-010 执行窗实录（e2e 保存点 1 次 400 丢参重跑即绿/双臂矩阵全绿/gate ALL GREEN 一次通过）；表头版本 v12→v13（SD-1004 指针）

> 方法论继承旧 jade-garden L3 差异表（design 30 §8）：差异不静默、逐项
> 记账（形态/处置/升级路径）。分类：**归档级**（双轨形态已对齐/不追）
> / **组装级**（当前装配差异，后续批次收口）/ **上游级**（auto-lang /
> engine / auto-down 侧缺口，本仓补丁或纪律规避）。

| # | 面 | vm 形态 | vue 形态 | 级 | 处置 / 升级路径 |
| --- | --- | --- | --- | --- | --- |
| D-01 | 编辑器代码字体/主题 | fence mono CJK tofu 债 + hljs 主题定格（auto-lang DEBTS 041 在册） | DOM 字体栈/hljs 主题随工程 CSS | 上游 | engine/auto-lang 侧债，本仓不修；观感差异接受 |
| D-02 | engine 平台面 | rust/VM 平台面 experimental（engine ARCHITECTURE §2） | 契约面（出口 1.0 冻结）稳定 | 上游 | engine 稳定化后续；断言域=契约面，深层行为不锁 |
| D-03 | 编辑器播种/切换 | `key: active_key` 重挂载播种（key 变即重读 content:） | **同左（2026-09-22 双轨归一）**：上游 PLAN-682 F2 原生直发 `:key="store.active_key"`（含 F-682-M1 布局臂双 :key 根修），补件③退役——双轨均 key 变重挂载播种 | 归档 | 切换可见延迟（0.5-1.95s，aura idle tick 节拍）为 D-03 自身残余，budgets `doc_switch_visible` 行互指；节拍改造 unlock 留上游 |
| D-16 | vm 首开编辑器全局初始化延迟 | 首个编辑器挂载付全部进程级一次性成本：two-face/syntect 注册表构建（首次触碰内嵌语法数据页）+ 围栏语言 onig 正则编译（debug 构建秒级，auto-lang highlight.rs 注释在册）+ 首次 CJK shaping | 不适用（vue 轨无此路径） | 上游 | 用户实录首开 2s+ 卡顿、二次开即恢复；热页缓存态复测首开 53ms 且 UI 线程零阻塞（心跳探针最差 4ms）——冷页缓存 + 系统负载（当时三兄弟 vm 实例在跑）放大所致。**PLAN-002 bench 复测（2026-09-21）**：进程内首开 54-415ms、热回访 press 往返 51-52ms 稳定（budgets `first_open_warm` hard 行锚）。优化路径（auto-lang 侧）：① boot 期预热——`spawn_syntax_warm_up` 机制已在库但只在编辑器创建时才点火（必输竞速），应在应用启动即跑常见语言 + 预建注册表；② release 构建（onig debug 正则编译秒级为 in-tree 文档明示）。**预算互指**：budgets.json `first_open_cold` 行 = ledger+blocked-upstream(D-16)；unlock = 供料包 §1 |
| D-04 | ~~生成 api client 四缺口~~ → 标量契约 | 不适用（vm 走 `use back.api` 直调） | 换基后契约全标量（str/int/bool）⇒ 旧四缺口（通配 URL/List\<T\>/JsonAny/map）**结构性消除**（regen-vue 断言守残留） | 归档 | PLAN-001 T-02 契约设计即规避；残余缺口见 D-10/D-15 |
| D-05 | actions 面 | actions{} 双轨可用（menubar/toolbar 快照锚 + keydown 回退层） | 同左（换基后 menubar/toolbar 已进入） | 归档 | PLAN-081「v0 无 menubar 最小面裁定 #4」随换基 supersede；残余=use 深度不对称（vue 只扫一级 use）——纪律：actions 只放根 widget |
| D-06 | ~~后端运行模式~~ | ~~split 外部 exe~~ → 换基后：自有 Auto src/back，merged 进程内直调 | HTTP（vite /api 代理 → `auto run --server vm -B`） | 归档 | PLAN-001 换基定版（supersede R-3）；a2r rust 引擎 = 上游缺口 F-R1（auto-edit PLAN-003 在册），不可用不阻塞 |
| D-07 | ~~JsonAny 契约字段~~ | ~~VM 存取损坏~~ → 换基后 front 全程零 JsonAny（frontmatter back 侧字符串层保留/拼回；read_wiki 只回 body） | 同左 | 归档 | PLAN-081 C-1「现读现传」绕法随边界内移退役；上游 JsonAny 装箱修复仍登记（旧 jade 潜在同坑在案） |
| D-08 | 状态读面 | 合并根状态裸读可用 | 裸读发射为未声明标识符（vue-tsc 拒绝） | 归档 | 定形 `.store.*` 单一读面（013 形态）；store computed 双轨皆坏已退役 |
| D-09 | 结构基线 | tests/baseline/structure-v1.txt 锁 state+snapshot（换基满状态；v0 留档） | 无对应（vue 轨 DOM 断言走 e2e） | 归档 | 断言域=两轨交集；vue 侧不复制 vm 基线形态 |
| D-10 | ~~vue-tsc v-for 兄弟模板作用域~~ | 不适用 | T-03 三症状（i/t/r；2.0.29/2.2.12 × vue3.4/3.5 全试无免）随 PLAN-671 新发射形态消失——2026-09-21 裸 strict 生成 + vue-tsc 2.2.12 实测零报错，v-for 拆分补丁与版本钉（vue 3.5.35+vue-tsc 2.0.29）一并退役 | 归档 | 源级消对（explorer 单按钮 + handler 分流）保留；tab 互补双分支发射仍在但零触发——源级若再引入同类兄弟对需重验 |
| D-11 | `.find` 闭包 split 硬崩 | merged OK；**`--no-merge` split 下 `.tabs.find(t => …)` 闭包使进程硬崩**（无 panic 输出；T-01 实勘，type/save 双复现） | 不适用（js 原生 find） | 上游 | 纪律：store 内 tab 定位一律 while 索引扫描（基座同款形态）；auto-lang 侧另立计划修复后解禁 |
| D-12 | autodown_editor 事件面 | key/content/final/oninput/on_focus（aura_view_builder.rs 实勘）——**无 oncursor/oncontextmenu**，无 code_editor_* 内建族 | 同左 | 上游 | StatusBar 行:列降级（docs 计数+脏标替代）；ctx_menu 组件不迁入（无右键锚）；menubar 编辑项 no-op+console 注记（编辑器内原生快捷键仍可用）；engine 事件面扩展后续批 |
| D-13 | vm 宿主内建 vue 运行期 | 不适用（宿主原生） | 类型面生成器自备（PLAN-671 ①+P2）；运行期 = regen-vue 补 `vm-natives.ts` 垫片（后装注入）。**2026-09-22 上游 682 F3 落地**：natives.ts R-tier 带先到先得守卫——垫片与 R-tier 顺序不再敏感，双方语义稳定（console_* 由 R-tier 管线或垫片，dialog_*/Process 语义垫片保持）；store 内联 `__vmOnly` 抛错桩仍需 no-op 补丁（QuitSaveClose 落盘后终止收敛） | 组装 | 垫片常驻（dialog 取消语义/终止语义上游未清偿）；R010 `tabs.remove` 同未清偿（补件②守） |
| D-14 | frontmatter updated_at 补写 | 旧 jade-garden-back 保存时服务端补 updated_at；换基 Auto back v0 **逐字保留 frontmatter**（不补写） | 同左 | 组装 | 六检查断言域不含 updated_at（磁盘原文+标记+frontmatter 三验）；补写属 wiki 域功能池后续批 |
| D-15 | 生成器残余缺口（换基面） | 不适用 | 双段 `${}` 插值错序（671 附a）、`--lenient` 通道（671 附b）、**tree JSON.parse（1784 ts_adapter 对 json.to_value 原生发射 JSON.parse——PLAN-002 补件②退役）**、**menubar 族真组件化（1784 起 Menubar/Trigger/Content/Item 组件族发射，裸 div 形态消失）** 已消；**仍存**：`tabs.value.remove` 直通（R010 INFO：receiver 非证数组即原样直通——regen-vue splice 补件守，上游未清偿）、deps/bps 部分同步 package load 告警 + dropdown-menu `open` prop schema 未吸收（warning 级不阻断）——**GalleryShell popover TS2307 已清偿**（上游 682 F4：gen-only 物化扫描落地，2026-09-22 补件④退役） | 上游 | 残余件上游清偿后补件断言 fail 提示撤除；auto-edit 侧七类链同日退役（d845e54 在册） |
| D-17 | vue 轨编辑器键入发射门控（PLAN-002 T-01 扩单实测登记） | 不适用（type_text 即发，INPUT_TEXT 整文回写同步可达） | 切档重挂载（:key 变）/外部换料（replaceDoc）后的编辑器实例：可打印键入**进引擎模型但不发射** `update:modelValue`，blur 时一次性调和冲刷（text-diff 路径）；回车块分裂为引擎死区（DOM 亦不分裂，replaceDoc 后实例）；首挂载实例逐键发射正常 | 上游 | e2e 以中性 blur（点 EXPLORER 头）= 确定性冲刷达成与 vm 即发同语义（D-17 机制注记入 spec）；store 段中插入/退格弧线全程可证、回车/退格净零断言（引擎局部）；unlock = 上游 engine（PLAN-677 在飞区）键入发射/块分裂路修复；供料包 §3 |
| D-18 | ~~上游快照投影属性双态~~ | ~~MCP snapshot 的 style/onclick 属性行随实例非确定发射~~ → **已归档（2026-09-22）**：上游 PLAN-682 F1 根修（view() 同步块接住探针填 computed + set_styled_vtree 拒降级覆盖），新 exe 三实例快照恒属性态逐字节一致（5201B×3 实证） | 不适用 | 归档 | 基线仪器 v2（state 逐字节 + vnode id 序列）保留——对确定投影与双态均稳健；根因与修复证据见 auto-lang PLAN-682（5f46d99fb/6830b0b2b） |
| D-19 | HTTP GET query UTF-8 解码（PLAN-003 T-04 实勘立案；PLAN-004 T-01/T-05 POST 适配注记） | split 形态（`--no-merge` AutoVM HTTP back）：服务端对 GET query 的 **UTF-8 百分号序列不解码**（`%20`/`%2F` 简单转义解码正常——ASCII+空格路径可用；CJK 编码路径 `exists`/`read_wiki` 全败——serve-back 8251 探针实录：encoded CJK → `exists:0`/`read_wiki:""`）→ HTTP 通道（split/vue）打开 CJK 文件名的档全败。**PLAN-004 适配**：检索面走 **POST**（`search_wiki` /api/search_wiki——write_wiki 同款双参形态，body CJK 已证），CJK 查询**双臂** 200 命中（T-01 探针六案 + T-04 双臂矩阵/e2e 实录）；GET query 解码缺口本身不变 | 同左（ts_adapter client 同走 query 串；POST client body 同证） | 上游 | **先在缺口**（非 PLAN-003 引入——vue 轨今日点 CJK 树行同败，此前检查单未覆盖 CJK 开档）；link 面板反链/出链行含 CJK 路径首测暴露。处置 = 检查单 CJK 导航子步仅 vm merged 臂（进程内直调无此环节；D-17 轨内差异注记先例），跨轨步骤全走 ASCII 路径；检索域已 POST 适配（SD-401）；unlock = auto-lang HTTP 层 query UTF-8 解码修复（供料包候选项） |
| D-20 | AutoVM/生成器原语面（PLAN-003 T-02..T-04 实勘集） | ①`fs.walk_files` 在册未接 .at 调用面（`Undefined symbol` 探针）——walk 改走已证 `fs.tree`；②**列表元素链式方法调用错乱**（heap 接收者 CALL_SPEC 错位——`segs[i].split_once(..)` 返回垃圾长度 pairlen=7 实录）→ 元素先拷局部变量再调方法；③**CJK heap 串 `.length` 字节语义**（TAG_STRING 臂=字符数 PLAN-055，heap 臂=字节——ASCII 掩盖）→ 定长截断禁用，split 法替代（strip_ad 同族修复已落）；④**store 上下文 `json.to_value` 顶层 map 产物入态 = 裸 vmref 损坏**（dump `link_pages: <vmref> (unknown)`、.len() 消费即零；App 模型 + 顶层数组 = Init ft_nodes 已证健康——C-1 亚型精确定位）→ 链接态居 App 模型 + link_index 顶层裸数组契约 | ⑤**ts_adapter 对 identifier 实参 `json.to_value(j)` 静默发射恒等 `out = j`**（call-arg 形 `json.to_value(f(..))` 才发射 `JSON.parse(await …)`——Init tree 同款）→ 双取形 workaround（原文态/解析态各取一次，fetch 幂等） | 上游 | 各项纪律规避已在源注记（wsys.at/front app.at）；①②④⑤ unlock = auto-lang / auto-down（ts_adapter）侧修复（供料包候选项）；⑥ 悬空链接 `target_path:""` 误配空 active 反链——本仓派生卫语句已修（语义级，非上游件） |
| D-21 | serve-back HTTP POST 负载窗丢参（PLAN-003 T-05 gate 实勘留观；PLAN-004 T-05 执行窗零复现；PLAN-004 复审 F-R1 进程死亡形态补记；PLAN-005 T-04 负载窗 7 失败实录扩记） | gate 语境（前置 vm 双臂矩阵 + vue build 重载后即跑 e2e）：quit 保存的 `write_wiki` POST 间歇 400 `{"error":"missing param \`path\` for POST /api/write_wiki"}`（服务端参数装配丢 path——api-err-body 实录；3 次 gate 2 次复现）。**PLAN-004 执行窗（2026-09-22）：gate 首跑 ALL GREEN + vm 双臂 4 连跑 + e2e 6 连跑 + search_wiki POST 双臂全过——零复现**。**复审窗（同日）新形态一例**：serve-back 后端进程死亡（ECONNREFUSED，探针连发窗内一次）；专项复现 ×9（含同参 limit=99 与更苛 51/0/-3）不可复现。**PLAN-005 执行窗（2026-09-22）：e2e 保存点 7 失败实录**（check 5 ×6 + check 9 ×1，≥2 例显式 400 丢参 api-err-body；**重跑即绿**，判绿窗 e2e 6/6 + gate ALL GREEN）——窗口期本机**家族会话同机并行实勘**（lang-692 `run -r vue --server vm` + musk-084 release `run --render vm` 与本仓测试同机在跑，非本会话进程未触碰）；PLAN-005 保存窗并发流 2→3（ActSave 增 TreeRefresh——G3 接线）为边际增量评估，**时序实验两枚负结果**：try 包裹 store.Save() 不发射 await（见 D-23②）→ vue 轨保存写 POST 与同 handler 读 GET 的并发窗属生成器形态上限，本仓无源级定序通道 | 同左（同 HTTP 面） | 上游 | 疑似 AutoVM HTTP 服务端负载下竞态两形态：①请求体读取/参数装配丢参（400）②服务进程偶发退出（连接拒）——均低频、独立负载窗不复现；**复现率与同机负载正相关**（家族会话并行窗 PLAN-003 2/3 gate、PLAN-005 7 失败；独占窗 PLAN-004 零复现）；处置 = README 无-RESULT/失败重跑口径覆盖 + 判绿连跑计数如实记 + 留观供料（unlock = auto-lang HTTP 层）；非 PLAN-002..007 任一切片引入（PLAN-005 保存窗增量已评估为边际）。**PLAN-006 执行窗（2026-09-23）v9 扩记**：e2e 24 跑 13 绿/11 失败全数签名吻合（400 丢参 ×9[check-5 保存点/quit 保存点] + ECONNRESET ×2[check-5]），重跑即绿，最长连绿 4；**隔离 serve-back 连发实证 write_wiki 50/50 + read_wiki 20/20（参数装配本体健康——400 仅 vite 代理 e2e 语境的突发簇形态，簇内连续跑易中、长冷却后过[gate 第 3 跑过]）——v8 的"负载正相关"精化为"代理链路语境 × 突发簇"**；vm split 臂早崩新形态 2 次（check-2 树行 6s 超时，state=ready 而 ft_nodes/ws_name 空——HTTP 树取空响应疑同窗，独占重跑即绿）。**PLAN-007 执行窗（2026-09-23）v10 扩记**：T-03 窗 e2e check-10 面板行断言 1 败[重跑即绿]；T-04 窗 1 轮 save 磁盘标记失败[失败点漂移签名] + 1 例 HTTP 400 瞬时 pageerror——重跑连绿；最终 gate ALL GREEN 一次通过；file 组零失败漂移。**PLAN-008 执行窗（2026-09-23）v11 扩记**：四形态实录——①merged 臂**进程死亡首例**（meta 组轮询中 ECONNREFUSED，app 进程消失[前此进程死亡仅 serve-back 面]；重跑即绿）；②merged 臂 check-10 面板行渲染窗 1 次（backlinks_open=true + bl_rows 3 元素在态而快照无面板行——v9"state ready 而行空"同签名）；③split 臂 **menubar popover 内容窗** 2 次（T-01 回归窗：视图菜单开而「切换反链」项 6s 未现——popover 内容渲染滞后新形态）；④**gate 语境 e2e 保存点连发簇**（e2e 窗口 23 跑 8 绿、败点全数 check-5 保存点 400 missing param `path`[api-err-body 两次实锤；POST 先于刷新 GETs——非 PLAN-008 新增 TagsRefresh fetch 所致]，最长连绿 3；独占/探针窗同一簇态；**gate 第 8 跑 ALL GREEN**，前 7 跑败点 e2e 段 ×6 + 矩阵段瞬态 ×1 如实记）；ActSave 刷新链并发流 3→4（TagsRefresh 同触点——PLAN-005 2→3 边际增量同口径评估，④ 实录中 POST 先于 GETs 故非本增量致因，留观）。**PLAN-009 执行窗（2026-09-23）v12 扩记**：三形态实录——①merged 臂 10m 首步 menubar popover 内容窗 ×1（「切换反链」项 6s 未现——v11③ 同款，重跑即绿）；②split 臂 check-10 面板行渲染窗 ×1（bl_rows 在态而行快照未渲染——v11② 同款签名，重跑即绿）；③merged 臂 check-14 tags 7 行渲染窗 ×1（基线首锁跑——面板开后立即快照取空行集，重跑即绿）；三窗后双臂全跑 + e2e 5 连绿 + **gate 首跑 ALL GREEN** 连续达成；ActSave 刷新链并发流 4→5（MentionsRefreshOf 同触点——PLAN-005 2→3/PLAN-008 3→4 边际增量同口径，留观）。**PLAN-010 执行窗（2026-09-23）v13 扩记**：e2e 保存点 1 次 400 丢参（missing param `path`，D-21 典型签名），独占重跑即全绿；vm 双臂矩阵 16/16 + 15/15 连跑全绿；gate 跑全部一次通过；alias 与 linkify 组零失败漂移 |
| D-22 | 查找切片原语面三项探针定谳（PLAN-004 T-01..T-04 实勘；全部通过=归档级非差异） | ①**str.to_lower/contains VM+HTTP 全通**（musk_vm_track_tests.rs:223 在册 + merged 直调/split HTTP 六案实测）；②**input 控件 MCP 自动化通道通**（convert_input 核心控件白名单 validate.rs:556 + aura_view_builder.rs:9721 `convert_input`；autoui_action type_text 直达 input——find_q 态断言+行过滤全链可用，探针 D）；③**onenter 事件面双轨在册**（vm convert_input onenter→on_submit aura_view_builder.rs:9758-9760 + textarea Enter 镜像 10057） | ①**ts_adapter 字符串方法映射表在册**（ts_adapter.rs Plan 053 M1：`to_lower→toLowerCase`/`contains→includes`/`trim` passthrough=JS 原生同名同义——探针 A 构建期 gen 源检 App.vue:408/413 复核）；②**onenter→@keyup.enter 在册**（vue.rs:15909/16428；Input.vue 根原生 input attrs 透传可达——gen 源检 App.vue:795 实证，探针 C）；③input 真 DOM fill() 即发 input 事件（v-model 无门控——D-17 仅编辑器组件） | 归档 | 三项探针（A/C/D）全通，无双轨差异需登记；SD-401 大小写/触发面均按通态定文。观测项记账：search 装配 O(P²)（v0 规模接受，上量换 StringBuilder native 160 族）、walk depth=4 内部常量（与快开/链接三面同界，参数化随 watch 批）、limit=99→50 钳分支语料不可观测（总命中 ≤4） |
| D-23 | 建页切片实勘集（PLAN-005 T-01..T-04；①②上游级纪律/缺口，③④归档级通态） | ①**`.replace` 次数语义双轨未证**（JS 串参=首现替换 ts_adapter.rs:1219；VM 全量与否未证）→ **while-contains 收敛惯用法**为纪律（每字符 `while t.contains(c) { t = t.replace(c, ...) }`——首现/全量两态皆收敛；create_page 清洗九字符六案双臂实测通过；**replace 变量实参 VM 可用**同证——实参取 List 元素先拷局部，D-20② 纪律形态）；②**try 包裹 handler/store 调用不发射 await**（ts_adapter try-体 await 仅限 api-client 调用——`try { store.Save() }`/`try { TreeRefresh() }` 均仍直发，T-04 负结果实验两枚）→ vue 轨 handler 内无法对 store/handler 调用定序；③vm 快照 alert-dialog 内容恒渲染（闭态也在树）且**弹层根为匿名 col**（无 alert-dialog 头标）→ 测试定位按内容锚（弹层钮 = 全树唯一钮的父 footer 行兄弟域，多弹同名钮 findFirst 首现必误中）；④**replace 惯用法双侧收敛等价**（merge 臂进程内 + split HTTP 六案返回值逐案一致——probe_create.mjs 双臂对读） | ①attr 单段 `${}` 插值 button `text:` 双轨通（span 内容插值同族发射——gen 源检 `{{ r.target }}` 同构）；②vue 编辑器 **markdown 渲染：`# ` 等标记符不落 DOM**（`# NewPage` 渲染为标题文本 NewPage）→ DOM 断言用渲染文，逐字节断言走磁盘（e2e 10c 实勘） | 上游 | ①VM 语义定谳后可简化惯用法为单次调用（unlock = auto-lang VM `.replace` 语义文档化/对齐——供料包候选）；②unlock = ts_adapter 调用面 await 发射扩展（handler/store 调用定序——供料包候选；D-21 保存窗并发的本仓侧终解）；③测试面纪律非缺陷（smoke/matrix pressInCreateDialog 形态）；④SD-501 清洗规则按惯用法收敛态定文 |
| D-24 | 改名切片实勘集（PLAN-006 T-01..T-04；①②④⑤上游级缺口/纪律，③上游级缺口） | ①**`File.copy` 解析层不可调**：`copy` 为 Auto 硬关键字[auto-lang token.rs:397，Plan 122 弃用参数模式]，dot 访问解析器拒绝[parser.rs:2429 白名单无 Copy]→ 含该调用的模块 **boot 即 fatal**（探针实录"error at 476:13 … got Copy"）——别名双表在册[native_catalog 1007/2602]≠可调，**较 D-20① 链接层缺口更深一层（解析层）**；`File.delete`/`File.rename`（后者无 .at 别名）非关键字；②**`File.delete` 恒返 0 吞错**（stdlib shim 忽略 remove_file 结果）→ exists 双复核必须（rename_page_impl 第④步内建）；③**menubar-item 状态相关 enabled boot 冻结**：boot 渲染定 handler 有无，状态变化不重评（开档后仍无 handler——vnode 一次创建实录），复合式 `!`/`&&` 更致节点整丢——基座保存项同病在案[其矩阵面走 toolbar 钮从未暴露] | ①vue 侧 dialog-cancel/action 反 Button 化（outline/ghost 预设）+ schema S002 warning——**与 vm 死标签构成轨间不对称**；②ts_adapter **参数名保留字直发**（`new` 形参 → vue-tsc TS1109 语法错，TabsRenamed 实录；wsys 侧 `new` 参数不受影响——back 模块 VM 解释不产 TS） | 上游 | ①改名 = read_text+write_text+delete 组合定文（SD-601——探针 A 裁定 fallback①，字节整迁等价）；供料候选升级：`File.rename`/.at 别名 + `copy` 关键字冲突解除[dot 访问白名单扩或关键字退役，两向任一]；②供料候选：shim 返真实结果或文档化恒 0 语义；③**纪律：menubar 项不挂状态相关 enabled**——禁用语义由 handler 守卫兜底 + action enabled_if 权威面（本仓 file.rename 落地形）；unlock = menubar popover 状态重渲（供料包候选）；④弹层双钮纪律 = dialog-footer 内普通 button（dialog-cancel/action 禁用——T-02 探针 B 定谳）；⑤参数名禁保留字（本仓 old_path/new_path 落地形；unlock = ts_adapter 保留字规避——供料包候选） |
| D-25 | file 切片实勘集（PLAN-007 T-01..T-05；①上游级缺口，②③归档级测试纪律） | ②**vm menubar-item 被 Popover lowering 渲染为 `button` 头**（`button #vnode "删除…" { onclick: __menubar_item("删除…") }` 实录——`menubar-item ` 前缀不成快照头，菜单项与普通钮同面匹配，matrix pressButton 同口径；冒烟调试 dbg 转储实录） | ①**ts_adapter App 上下文未知字段 `.console` 裸发射全局赋值**：App handler（App 无 console 模型字段）`.console = console_lines()` → 直发 `console = console_lines()` → **覆写 window.console 为字符串**——vm 轨无此分野（widget 本地静默赋值无害）；后果链 = 后续任何 `console_log` 垫片[globalThis.console_log 内 `console.log` 抛 TypeError] → **handler 中断**（store.Open `push → console_log → TabActivate` 中段断链实录：tab 压入激活失败——PLAN-007 file 组 e2e 首次行为级暴露）；**PLAN-006 起潜伏**（RenameGo 拒分支 case-only 案每轮 e2e 必触发——`[pageerror] console.log is not a function` 即此，PLAN-006 记录为非致命观测未归因）；②lucide-vue-next 0.312 icon class = `lucide-<kebab(name)>`（createLucideIcon.js 源检——`svg[class*="lucide-plus"]` 子串定位；EXPLORER ＋/tab ＋ 双 Plus 同 class，DOM 序 .first() 消歧[EXPLORER 列先于 tab 条渲染]） | 上游 | ①**纪律：App 上下文 handler 禁写 `.console = console_lines()`**（本仓 8 处已移除——store 侧 30 处 `.console.value = console_lines()` 合法保留，面板刷新由 store handler 自维护）；unlock = ts_adapter 未知字段诊断/拒发（`.console` 这类 App 无主字段应报错而非全局发射——供料包候选，与 D-24⑤ 保留字直发同族）；②③测试面纪律非缺陷 |
| D-26 | mentions/inline 切片实勘集（PLAN-009 T-01..T-05；①③执行期校正/断言通道落定，②上游级已知面新实例） | ①**无分野**（handler 同步发送——LinksRefreshOf 完成后 MentionsRefreshOf 方启动，.bl_rows 读恒现算）；③**面板关零 fetch 行为等价断言通道**：merged 臂网络不可观测（§10.1 落定）——关面板 → 激活变更 → mention_rows state dump 行恒旧值 + 段不渲染双断言（vm_matrix 10m ⑥在册） | ②**handler 间无 await 跨 handler 数据依赖竞态**（D-23② 新实例）：ActBacklinks 内 `LinksRefreshOf()`/`MentionsRefreshOf()` 并发发射——单取形 search_wiki 先于双取形 link_index 双 fetch 返回，MentionsRefreshOf 读 `.bl_rows` 吃到 pre-refresh 陈旧投影（e2e check-10 实勘：已链源三行漏排重误入提及段，修复前 5/5 必现）；**修复 = MentionsRefreshOf 自派生已链源集**（`backlink_rows_of(.link_pages, active)`——.link_pages 为最近完成索引刷新产物，与面板在显反链行同鲜度——排重与所见一致口径）；结构性语义等价：两轨同输入同输出，仅 vue 时序窗口不同 | 上游 | ①行内 #tag **前导空白语义**执行期校正（SD-901 忽略三则→四则）：语料实勘 `\[\[Tasks#block-project-a\]\]` 转义链接锚面——`X#y` 连续文本（链接锚/普通串）非 tag（Obsidian 行首/空白前导语义）；prev 尾字符 ends_with 链判（禁 .length 取尾——D-20③ 同族）；语料已知答案零漂移成立（block-project-a/b 不入集——probe_inline_tags 七案负向面锁）；②unlock = D-23② 同款（ts_adapter handler 间 await 发射——供料包候选）；**纪律：跨 handler 数据依赖一律自派生或单口内化**（本仓 MentionsRefreshOf 落地形）；③断言通道纪律入 vm_matrix（行为等价双断言形态可复用） |
| D-27 | 别名与转为链接切片实勘集（PLAN-010 T-01..T-05 + 复审修复窗；①上游级快照基线展开，②上游级纪律/时序定序，③归档级通态，④测试面切片纪律[复审 F-R10-1 修复期实勘]） | ①**快照基线节点数展开**：上游 auto-lang PLAN-089（7183ca386）修复 MouseArea 按键链将 MouseArea 展开为 `Container + col + text`（vnode_converter 及 snapshot_builder 扩充），致快照节点由 146 扩至 149；store 状态段 100% 逐字节零模型漂移，属计划内重锁；②无分野（handler 顺序执行）；③无分野；④无分野 | ①不适用；②**vue handler 跨调用无 await 竞态**（D-23②/D-26② 延续）：LinkifyGo 连调 LinksRefreshOf 与 MentionsRefreshOf 并发时，MentionsRefreshOf 会在 link_pages 刷新前读旧索引导致转链提及行残留 → **修复：MentionsRefreshOf 链式移入 LinksRefreshOf 内部完成处**，保证链接索引更新后才刷提及；③**autodown markdown 标题渲染**：`# Caller` 标题在 DOM 渲染为 text `Caller`（h1），DOM 断言需用 `toContainText('Caller')`；④**快照段切片必须有界**：vm 快照文本 DFS 序中 filetree 居反链面板之后——面板段（未链接提及 锚起）无界 slice 会误扫树行同名文本（F-R9-4 案首版 fr94Down 恒假阴实录：UI 造档进树 vs 10m fs 造档不进树的分野）；段断言 = `slice(i, i+400)` 有界 + 空态标记（（无未链接提及）与行互斥渲染）作判别面 | 上游 | ①基线更新保持；②unlock = D-23② 同款（ts_adapter 调用面 await 发射）；③测试面纪律；④vm_matrix 段切片纪律（树行在册素材案必须有界切片） |


## 增记规则

新差异：先登记（含形态与级），再决定处置（补丁/纪律/上游/接受）；
升级路径完成时把级改归档并留证据链接。差异表后置面（ PLAN-080 尾事）
后续按此表扩展。
